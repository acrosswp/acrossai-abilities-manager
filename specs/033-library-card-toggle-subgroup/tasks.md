---

description: "Task list for Feature 033 — Library Card Toggle + Optional Sub-Group Display"
---

# Tasks: Library Card Toggle + Optional Sub-Group Display

**Input**: Design documents from `specs/033-library-card-toggle-subgroup/`
**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [memory-synthesis.md](./memory-synthesis.md), [security-constraints.md](./security-constraints.md), [architecture-review.md](./architecture-review.md)

**Tests**: REQUIRED — Constitution §VII Definition of Done ("Unit tests written and passing for all new logic") and spec.md CHANGE-B-6 list specific PHPUnit cases.

**Organization**: Tasks grouped by user story (US1 = P1 MVP, US2 = P2, US3 = P3) for independent implementation and verification.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Different file from siblings, no incomplete-task dependency — safe to parallelize.
- **[Story]**: Maps to spec.md user stories (US1, US2, US3).
- Exact file paths in every task.

## Path Conventions

Single WordPress-plugin source tree under repo root:

- PHP: `includes/Modules/Library/`
- JS: `src/js/ability-library/components/`
- SCSS: `src/scss/ability-library/`
- PHP tests: `tests/phpunit/Modules/Library/` (NEW — directory does not yet exist; first PHPUnit task creates it)
- JS tests: `tests/jest/ability-library/` (NEW)
- Build output: `build/js/ability-library.js` (auto-generated by `npm run build`; no hand edits)

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Confirm the dev environment can build and run all gates this feature relies on.

- [x] T001 Verify dev environment from repo root: run `composer install` (PHP deps + WPCS + PHPStan), run `npm install`, then run `npm run validate-packages` — all three must succeed before any code changes start.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Cross-story prerequisites that MUST complete before user-story work.

**None for this feature.** US1 (a one-line JSX comment + Jest test) and US2 (PHP + JS + CSS + tests) are independent. US3 is verification work that runs after US1 + US2 land. The plan introduces no new infrastructure, no new tables, no new endpoints — no foundational tasks required.

**Checkpoint**: ✅ Skip directly to Phase 3.

---

## Phase 3: User Story 1 — Lock down All/Specific checkbox visibility (Priority: P1) 🎯 MVP

**Goal**: Codify the existing behavior at `src/js/ability-library/components/LibraryCard.js:72` (`enabled && mode === 'specific' && slugs.length > 0`) as a documented contract and add regression coverage so a future edit can't silently break it.

**Independent Test**: Mount `<LibraryCard>` in Jest with `mode='all'`, assert `.acrossai-library-card__slugs` is `null` in the DOM. Re-render with `mode='specific'`, assert the panel IS mounted. Switch back to `'all'`, assert the panel unmounts again. No PHP, no CSS, no add-on code required.

### Implementation for User Story 1

- [x] T002 [US1] **Turn 3 revision**: the visibility guard is now `enabled && slugs.length > 0 && expanded` (added the `expanded` clause from a per-card `useState(true)`). Added a `Button` (from `@wordpress/components`) with `chevronUp`/`chevronDown` icons (from `@wordpress/icons`) to the card header — controls the `expanded` state. `aria-expanded` + tooltip label ("Expand ability list" / "Collapse ability list") satisfy FR-020. The per-row `mode === 'specific' ? <CheckboxControl /> : <div className="…__slug-readonly">` branch from turn 2 is unchanged. New SCSS rule `&__disclosure` added (turn 3); `&__slug-readonly` rule added (turn 2); `&__subgroup-heading` rule added (turn 1).

### Tests for User Story 1

- [x] T003 [P] [US1] **Revised turn 3**: `shouldRenderSlugPanel(enabled, slugsLen, expanded)` now takes the `expanded` clause. A new predicate `shouldShowDisclosureButton(enabled, slugsLen)` covers the chevron-presence guard. `shouldRenderInteractiveRows(mode)` (turn 2) and `nextSubKeysOnModeChange` are unchanged. **13 cases total**, all pass. Also updated `groupBySubGroupPreservingOrder.test.js` jest.mock list to add stubs for `Button`, `Fragment`, `useState`, and `@wordpress/icons` so the helper import still works after the LibraryCard.js imports grew.

- [x] T004 [US1] **Result (turn 3)**: full ability-library suite 20/20 passed in 1.35s (LibraryCard 13 cases + groupBySubGroupPreservingOrder 7 cases).

**Checkpoint**: US1 contract locked in. Feature 033 has shipped its MVP.

---

## Phase 4: User Story 2 — Optional sub-group display layer (Priority: P2)

**Goal**: Let add-on authors declare `args['sub_group']` on an ability and have the Library page render a small `<h4>` heading above the matching slug checkboxes inside the Specific panel — without altering saved-config shape, REST contracts, or execution paths.

**Independent Test**: Wire a temporary add-on that returns `args['sub_group'] => 'core'` (and `'plugins'`, `'themes'`, `'debug-log'`, `'config'`) for the File Manager category's abilities. Reload the Library page and verify five `<h4>` rows appear in registration order. Tick checkboxes under sub-groups, save, and `wp option get acrossai_library_config --format=json` shows zero `sub_group` keys anywhere in the JSON. Remove the declarations; the Library page falls back to a flat checklist with no console errors.

### Implementation for User Story 2 — PHP layer

- [x] T005 [US2] Edit `includes/Modules/Library/Ability_Definition.php`. Extend `push_definition()` to: (a) read `$sub_group = isset( $args['sub_group'] ) ? (string) $args['sub_group'] : '';`, (b) when `'' !== $sub_group`, add `'sub_group' => $sub_group` and `'sub_group_label' => ucwords( str_replace( '-', ' ', $sub_group ) )` to the pushed row, (c) update the class-level docblock at lines 17–21 with the new sentence: `Optional: args['sub_group'] adds a display-only sub-heading inside the Library Specific panel. Does NOT affect saved config or execution.` New docblocks for new methods must start with "The " (BUG-PHPCS-DOCBLOCK-CAPITAL).

- [x] T006 [US2] Edit `includes/Modules/Library/AcrossAI_Ability_Library_Registry.php` — all in one commit on this file: (a) add `'sub_group'` to the `ALLOWED_ARGS_FIELDS` private const (after `'category'`, before `'execute_callback'`) with an inline `// Feature 033 — optional display-only sub-heading` comment; (b) add a new `private const OPTIONAL_FIELDS = array( 'sub_group', 'sub_group_label' );`; (c) inside `validate_and_normalize()`, after the existing required-field loop builds the validated `$entry`, branch on `isset( $item['sub_group'] ) && '' !== $item['sub_group']` to attach `sub_group` + `sub_group_label` per the plan's "Field Routing Table"; (d) add a `private static function sanitize_sub_group( string $raw ): string { return AcrossAI_Ability_Library_Config::sanitize_key_field( $raw ); }`; (e) extend the `apply_filters` docblock at lines 103–116 to mention the two new optional keys. Use `wp_kses_post()` ONLY when an explicit `sub_group_label` is supplied; auto-derive otherwise (SC-033-02). Empty after sanitize = treat as absent (SC-033-03 / FR-018).

### Implementation for User Story 2 — JS layer

- [x] T007 [P] [US2] Edit `src/js/ability-library/components/LibraryPage.js`. Inside `groupDefinitions()` (lines 13–37): extend the destructure to add `sub_group: subGroup, sub_group_label: subGroupLabel`, and extend the `group.slugs.push(...)` call to add `subGroup: subGroup || ''` and `subGroupLabel: subGroupLabel || ''`. Order preservation MUST be unchanged.

- [x] T008 [US2] Edit `src/js/ability-library/components/LibraryCard.js` (same file as T002 — sequential dependency on T002). Three changes: (a) extend the existing `@wordpress/element` import to include `Fragment` (add the import if absent — current file does not import from `@wordpress/element`); (b) add a **named export** `export function groupBySubGroupPreservingOrder(slugs)` per the plan's helper signature (PATTERN-NAMED-EXPORT-JEST — must be importable by Jest without rendering React); (c) replace the existing `{slugs.map(...)}` block inside the `enabled && mode === 'specific' && slugs.length > 0 && (...)` guard with the grouped render described in plan.md → CHANGE-B B-4 (Fragment per group keyed by `subGroup || '__ungrouped'`, `<h4 className="acrossai-library-card__subgroup-heading">` rendered only when `subGroup !== ''`, CheckboxControl onChange wiring unchanged — still writes `sub_keys[slug] = bool`).

### Implementation for User Story 2 — CSS layer

- [x] T009 [P] [US2] **Turn 3**: `&__subgroup-heading` (turn 1) + `&__slug-readonly` (turn 2) + `&__disclosure` (turn 3 — chevron button styling: `flex-shrink: 0; color: $muted`). Also added `gap: 8px` to `&__header` so the chevron / toggle / radio spacing is consistent. to the `.acrossai-library-card` block in `src/scss/ability-library/admin.scss` (place it after the existing `&__slugs` block at line 89–101): `margin: 12px 0 4px; font-size: 13px; font-weight: 600; color: var(--wp-admin-theme-color, #1e1e1e); text-transform: none;`. Do NOT create a new SCSS file.

### Tests for User Story 2

- [x] T010 [P] [US2] Create `tests/phpunit/Modules/Library/Test_Ability_Definition.php` — **5 tests, 18 assertions, all pass.** Added 3 stubs to tests/bootstrap.php (sanitize_key, get_site_option, update_site_option) for the Registry/Config tests; wired `library-unit` testsuite into phpunit.xml.dist via explicit `<file>` entries (PHPUnit 10 defaults to `*Test.php` directory autodiscovery; our files use `Test_*.php` prefix). (and the parent `tests/phpunit/Modules/Library/` directory). Two test methods per spec CHANGE-B-6: `test_push_definition_omits_sub_group_when_absent()` (assert no `sub_group` / `sub_group_label` keys in row), `test_push_definition_includes_sub_group_when_present()` (assert `sub_group === 'core'` and auto-derived `sub_group_label === 'Core'` for `'core'` input). Instantiate a concrete `Ability_Definition` subclass inline per test — do NOT use a typed class property in `set_up()` (BUG-PHPUNIT-TYPED-PROPERTY-SETUP).

- [x] T011 [P] [US2] Create `tests/phpunit/Modules/Library/Test_Ability_Library_Registry.php`. **6 tests, 16 assertions, all pass.** Implementation note: tests call `validate_and_normalize()` via Reflection because the public `collect()` path is filter-driven and the bootstrap's `apply_filters` stub returns the first value unchanged (so feeding test data through `add_filter` is not viable under this bootstrap). Three test methods: `test_registry_strips_invalid_sub_group()` (assert `'!!! bad-input ###'` is sanitized via `sanitize_key()`); `test_registry_allows_sub_group_in_args_allowlist()` (assert a definition passed through with `args['sub_group']` retains the key on the validated `args` array); `test_registry_omits_sub_group_when_empty_after_sanitize()` (assert FR-018 / SC-033-03 — input that sanitizes to `''` produces a row without sub_group keys). Call `AcrossAI_Ability_Library_Registry::instance()` inline per test.

- [x] T012 [P] [US2] Create `tests/phpunit/Modules/Library/Test_Ability_Library_Config.php` — **3 tests, 11 assertions, all pass.** Includes baseline shape regression and sparse-storage preservation tests in addition to the SC-033-04 stray-sub_group case. with `test_save_config_ignores_sub_group()` — call `AcrossAI_Ability_Library_Config::save_config()` with a payload that contains a stray `sub_group` field on an entry, then read the saved option via `get_site_option( 'acrossai_library_config' )` and assert the saved entry shape is exactly `{ enabled, mode, sub_keys }` — `sub_group` MUST NOT appear anywhere (SC-033-04, FR-011, FR-012).

- [x] T013 [P] [US2] Create `tests/jest/ability-library/groupBySubGroupPreservingOrder.test.js`. **7 tests, all pass.** Adds a missing-subGroup-property case as a bonus regression check. Import the named export from `src/js/ability-library/components/LibraryCard`. Cases: (a) input with no `subGroup` returns a single `''`-keyed group in original order; (b) input mixing ungrouped + 'core' + 'plugins' returns ungrouped first, then 'core', then 'plugins' (first-seen order); (c) duplicate `subGroup` across non-adjacent items keeps the first occurrence's position and appends later items to that group's `items[]`; (d) input where every item declares the same `subGroup` returns one group. No React mount.

### Quality gates for User Story 2

- [x] T014 [US2] Run `composer run phpstan` and `composer run phpcs` from repo root — zero errors on the three edited PHP files (`Ability_Definition.php`, `AcrossAI_Ability_Library_Registry.php`) AND on the new test files in `tests/phpunit/Modules/Library/`. **PHPStan level 8: exit 0, zero errors. PHPCS WPCS: 6 files, zero errors.**

- [x] T015 [US2] **Turn 3 rebuild**: webpack compiled successfully in 6.03s; `js/ability-library.js` 4.07 KiB (was 3.43 → +0.64 KiB for the Button / chevron icons / useState wiring) + `css/ability-library.css` 1.64 KiB (was 1.57 → +0.07 KiB for `&__disclosure`).

- [x] T016 [US2] Run `npx wp-scripts test-unit-js` — all Jest specs (US1 + US2) pass. **16/16 in 1.38s; 9 US1 + 7 US2.**

- [ ] T017 [US2] **DEFERRED — requires running Docker / wp-env.** Run Plugin Check workflow against the production surface (`wp-env run cli wp plugin check acrossai-abilities-manager --include-experimental`) — zero new errors / warnings on the modified PHP files. **Deterministic constituents already passed**: PHPStan level 8 clean, PHPCS WPCS clean, no forbidden functions introduced (no eval/extract/shell_exec/etc.), no new SQL (no `%i` concerns), no new `error_log()` calls (no WP_DEBUG_LOG guard work needed), edits confined to `includes/Modules/Library/` production surface, `wp_kses_post()` applied per SC-033-02 to explicit `sub_group_label` overrides. User to run Plugin Check directly when wp-env is up.

**Checkpoint**: US2 is fully functional and testable.

---

## Phase 5: User Story 3 — Existing add-ons continue to work unchanged (Priority: P3)

**Goal**: Verify that after US1 + US2 land, every previously-shipped add-on that does NOT declare a `sub_group` renders its Library cards identically to the prior release.

**Independent Test**: With the test plugin's add-ons unmodified, the Library admin page must show every category exactly as before — same number of checkboxes, same order, same labels, no headings, no console errors. Saved configuration round-trips byte-for-byte (SC-005 / FR-011).

### Verification for User Story 3

- [ ] T018 [US3] **DEFERRED — manual gate, requires running wp-env.** Manual smoke test on the local wp-env install: open the Library admin page, verify every category card without `sub_group` declarations renders as a flat checklist (no `<h4>` headings present); open DevTools console — zero warnings, zero errors. Repeat for at least three pre-existing categories (e.g. Plugins, Themes, Users). **Automated coverage that approximates this**: 16/16 Jest specs pass (LibraryCard visibility + groupBySubGroupPreservingOrder), including the "missing subGroup property is treated as ungrouped" case that mirrors the backwards-compat shape.

- [ ] T019 [US3] **DEFERRED — manual gate, requires running site + WP-CLI.** Verify saved-config round-trip via `wp option get acrossai_library_config --format=json` before/after a UI save. **Automated equivalent passed**: `Test_Ability_Library_Config::test_save_config_ignores_stray_sub_group_field()` (SC-033-04) directly asserts on the saved-option store via `get_site_option()` — stray `sub_group` in payload does NOT appear in saved data, and `wp_json_encode($saved)` contains zero `sub_group` substrings.

- [ ] T020 [US3] **DEFERRED — manual gate, requires running site + WP-CLI.** Grep the saved option for `sub_group`. **Automated equivalent passed**: `test_save_config_ignores_stray_sub_group_field` uses `assertStringNotContainsString( 'sub_group', wp_json_encode($saved) )` — exactly equivalent to the manual grep.

**Checkpoint**: Backwards compatibility verified. Feature 033 is shippable.

---

## Phase 6: Polish & Cross-Cutting Concerns

- [x] T021 [P] Update `AGENTS.md` "Ability Library" section (or add a new one-liner under the existing Library notes) with: `Optional args['sub_group'] on Ability_Definition is display-only — never changes saved config or sub_keys shape.` Keep it to a single line. **Added a new "## Ability Library" subsection after "Plugin Check & Code Quality Rules" with the canonical sentence + a follow-on about `args['sub_group_label']` overriding the auto-derived label.**

- [x] T022 Run ESLint on the three edited JS files. **Source-file lint** (`src/js/ability-library/components/{LibraryCard,LibraryPage}.js`): zero errors. **Test-file lint**: `no-undef` errors for `expect`/`test` are a **pre-existing repo-wide baseline issue** — `tests/jest/abilities/column-prefs.test.js` has 38 of the same errors. Feature 033 does NOT regress this baseline. Prettier formatting was auto-fixed in the two new test files via `npx wp-scripts lint-js --fix`. **Recommended follow-up (separate scope)**: add a `{ files: ['tests/jest/**/*.test.js'], languageOptions: { globals: globals.jest } }` block to `eslint.config.js` to eliminate the baseline `no-undef` errors across all Jest test files (out of Feature 033 scope).

- [x] T023 Run `npm run validate-packages` — must pass (Constitution §VII DoD). **Pass**: "No direct React imports found / @wordpress/element is available / No duplicate React packages detected."

- [ ] T024 **DEFERRED — manual gate, requires running site.** Walk every Acceptance Scenario for US1 (5 scenarios), US2 (5 scenarios), US3 (2 scenarios), and every Edge Case in `spec.md`. **Test coverage delta**: US1 scenarios 1, 2, 3 (panel mount/unmount + sub_keys reset) and scenarios 4, 5 (toggle-off + empty-category guard) are all covered by `LibraryCard.test.js` cases 1–6. US2 scenarios 1–4 (sub_group renders, sub_group does not enter saved data) are covered by the four PHPUnit Library tests + the grouping-helper Jest spec. US3 scenarios 1–2 are covered by the `Test_Ability_Library_Config::test_save_config_preserves_shape_when_no_sub_group_present` baseline + Jest case "missing subGroup property is treated as ungrouped". The remaining manual walk is for "renders in browser without console errors" / visual verification, which only the user with a running site can do.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 Setup**: No dependencies.
- **Phase 2 Foundational**: Empty — skip.
- **Phase 3 US1**: Can start immediately after Phase 1. Independent of US2 and US3.
- **Phase 4 US2**: Can start immediately after Phase 1. **T008 depends on T002 (US1)** because both edit `LibraryCard.js`. If US2 is implemented before US1, T002's comment must be added together with T008.
- **Phase 5 US3**: Verification only — depends on both US1 (T002 + T004) and US2 (T005–T017) being landed.
- **Phase 6 Polish**: Depends on all desired user stories being complete.

### Within Each User Story

- **US1**: T002 → T003 → T004 (test depends on the comment being present so the file under test is in its final shape; T003 itself does not assert on the comment).
- **US2 — PHP**: T005 → T006 (different files, but T006 references the new field added by T005; safer sequential). Optionally [P] if both authors coordinate.
- **US2 — JS**: T007 [P] → T008 (sequential because T008 also depends on T002 from US1; T007 is independent).
- **US2 — CSS**: T009 [P] — independent file.
- **US2 — Tests**: T010, T011, T012, T013 all [P] — four different new files.
- **US2 — Quality gates**: T014 → T015 → T016 → T017 — strictly sequential, must run after T005–T013.
- **US3**: T018 → T019 → T020 — strictly sequential (each manual verification builds on the prior state).

### Parallel Opportunities

- T007 (JS LibraryPage) is parallel with T005 / T006 (PHP).
- T009 (CSS) is parallel with all PHP and JS implementation tasks.
- T010, T011, T012, T013 (four test files) all run in parallel.
- US1 (T002–T004) and US2 PHP+test tasks (T005, T006, T007, T009, T010, T011, T012, T013) can be done by different developers concurrently — only the LibraryCard.js touchpoint (T002 + T008) requires single-developer ownership.

---

## Parallel Example: User Story 2 — Tests phase

```bash
# After T005–T009 complete, all four test files can run in parallel:
Task: "Create tests/phpunit/Modules/Library/Test_Ability_Definition.php with the 2 cases from CHANGE-B-6"
Task: "Create tests/phpunit/Modules/Library/Test_Ability_Library_Registry.php with the 3 sanitization cases"
Task: "Create tests/phpunit/Modules/Library/Test_Ability_Library_Config.php with the save_config no-sub_group case"
Task: "Create tests/jest/ability-library/groupBySubGroupPreservingOrder.test.js with the 4 ordering cases"
```

## Parallel Example: User Story 2 — PHP + JS + CSS in parallel

```bash
# T007 (JS) and T009 (CSS) run while T005/T006 (PHP) are in progress, on different files:
Task: "Edit src/js/ability-library/components/LibraryPage.js groupDefinitions() destructure"
Task: "Append &__subgroup-heading rule to src/scss/ability-library/admin.scss"
# T008 cannot start until T002 (US1) lands, because both edit LibraryCard.js.
```

---

## Implementation Strategy

### MVP First (US1 only)

1. Complete Phase 1 setup.
2. Complete Phase 3 US1 (T002 → T003 → T004).
3. **STOP and validate**: run `npx wp-scripts test-unit-js` — Jest contract test passes. Push branch, ship MVP.

### Incremental Delivery

1. Phase 1 → Phase 3 (US1 MVP) → ship.
2. Phase 4 (US2) → ship — sub-group display now opt-in for add-on authors.
3. Phase 5 (US3) → ship — explicit backwards-compat verification record.
4. Phase 6 polish → final commit.

### Solo-Developer Strategy

Recommended order if one person is doing all of it:
**T001 → T002 → T003 → T004 → T005 → T006 → T007 → T008 → T009 → T010 → T011 → T012 → T013 → T014 → T015 → T016 → T017 → T018 → T019 → T020 → T021 → T022 → T023 → T024.**

### Multi-Developer Strategy

- Developer A: US1 (T002–T004) → US3 verification (T018–T020).
- Developer B: US2 PHP (T005, T006) → US2 PHPUnit tests (T010, T011, T012) → T014.
- Developer C: US2 JS/CSS (T007, T009, T008 — waits for T002) → US2 Jest (T013) → T015, T016.
- All converge on T017 → Phase 6 polish.

---

## Validation Summary

- **Total tasks**: 24
- **Per story**: US1 = 3; US2 = 13; US3 = 3; Setup/Polish = 5
- **Parallel-friendly markers**: 6 ([P] on T003, T007, T009, T010, T011, T012, T013, T021 — count above includes test parallelism)
- **Independent-test coverage**: US1 → Jest mount/unmount assertion; US2 → wp option get JSON shape + add-on subclass round-trip; US3 → grep `sub_group` against saved option + diff against pre-change snapshot
- **Suggested MVP**: Phase 1 + Phase 3 (US1) — 4 tasks (T001–T004) — ships the visibility-contract lockdown alone
- **Format check**: every task is a `- [ ]` line with `T###`, optional `[P]`, optional `[Story]` label, an explicit file path, and a verifiable action

---

## Notes

- `[P]` tasks edit different files and have no incomplete-task dependency.
- US1 and US2 BOTH touch `LibraryCard.js` (in disjoint sections: T002 above the guard, T008 inside the slug-map block). They are sequential w.r.t. each other on that one file.
- `sub_keys` is the on-disk wire key — must NOT be renamed (plan.md "must NOT change" list; DEC-LIBRARY-CATEGORY-SLUG-REBRAND).
- Any new `error_log()` MUST follow PATTERN-WP-DEBUG-LOG-GUARD — wrapped in `if ( defined( 'WP_DEBUG_LOG' ) && WP_DEBUG_LOG )` with `phpcs:ignore` inside the guard (SC-033-07).
- The Reflection finding at `AcrossAI_Ability_Override_Processor.php:715` is **out of scope** — see spec.md "Out of Scope — Known Plugin Concerns" + plan.md "Known Plugin Concerns Outside This Feature's Scope". Do NOT add a task for it here.
- Commit after each task or logical group; never mix US1 and US2 commits — they should land as separate logical units.
